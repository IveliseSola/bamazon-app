var mysql = require("mysql");
var inquirer = require("inquirer");
var connection = requirer("./dbConnection.js");

connection.connect(function (err) {
    if (err) throw err;
    
    // functions here
});